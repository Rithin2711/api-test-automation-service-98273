# inputs directory

This directory is reserved for input artifacts such as:
- Swagger/OpenAPI files (e.g., swagger.json)
- Test case Excel files (e.g., tests.xlsx)

Note:
- This folder is optional at runtime; uploads are stored in the database via the StoredFile model.
- You may place sample or default input files here for local development.
